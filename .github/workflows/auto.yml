---
name: Autodeploy

"on":
  workflow_dispatch:
  push:
    branches:
      - master
jobs:
  deploy:
    runs-on: ubuntu-20.04
    if: github.event.pull_request.merged == 'true'
    steps:
      - name: report
        run: echo Run deploy!
