---
inputs:
  foo:
    requred: false
    default: false
runs:
  using: "composite"
  steps:
    - run: echo  foo is "*${{ inputs.somelist }}*"
      shell: bash
    - run: echo 'run only if true'
      if: ${{ inputs.foo }}
    - run: echo 'run only if false'
      if: ${{ ! inputs.foo }}
