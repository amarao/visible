---
inputs:
  actor:
    required: true
  sha:
    required: true
runs:
  using: "composite"
  steps:
    - run: echo ${{ inputs.actor }}
      shell: bash
    - run: echo ${{ inputs.sha }}
      shell: bash
